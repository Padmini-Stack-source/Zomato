apiVersion: v1
kind: Service
metadata:
  name: zomato
  namespace: production
spec:
  type: LoadBalancer
  selector:
    app: zomato
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
